@model IEnumerable<LMS.Web.ViewModels.TeacherViewModels.TeacherStudentListViewModel>
@{
    ViewData["Title"] = "Teacher Student Management";
    ViewBag.CardTitle = "Student Management";
}

<div class="container mt-4">
    <h2>My Students</h2>
    <div class="card">
        <div class="card-body">
            @if (Model.Any())
            {
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(m => m.First().FullName)</th>
                            <th>@Html.DisplayNameFor(m => m.First().StudentNumber)</th>
                            <th>@Html.DisplayNameFor(m => m.First().ClassName)</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var student in Model)
                        {
                            <tr>
                                <td>@student.FullName</td>
                                <td>@student.StudentNumber</td>
                                <td>@student.ClassName</td>
                                <td>
                                    <a asp-action="Details" asp-route-id="@student.StudentId" class="btn btn-primary btn-sm">View Profile</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p>No students found for the classes you teach.</p>
            }
        </div>
    </div>

    @* @section Scripts { *@
    @*     <script> *@
    @*         // Optional: Add any JavaScript if needed (e.g., for table sorting or filtering) *@
    @*         $(document).ready(function () { *@
    @*             $('.table').DataTable(); *@
    @*         }); *@
    @*     </script> *@
    @* } *@
</div>
